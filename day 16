int main() {
	int t,temp=0;
	cin>>t;
	while(t--)
	{   int N, temp=0;
	    cin>>N;
	   int arr[N];
	   for(int i=0;i<N;i++){
	       cin>>arr[i];
	   
	       temp=max(temp,arr[i]);
	   
	}
	cout<<temp<<endl;}
	return 0;
}
