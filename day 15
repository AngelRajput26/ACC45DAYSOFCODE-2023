int main() {
	int t;
	cin>>t;
	while(t--)
	{
	    int n,x;
	    cin>>n>>x;
	    if(n==0 or n==x)
	    { cout<<"0"<<endl;}
	    else if(n/2>=x)
	    {cout<<x<<endl;}
	    else
	    { cout<<n-x<<endl;}
	    
	}
	return 0;
}
